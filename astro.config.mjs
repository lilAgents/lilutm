// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default). Deploys to Netlify with zero adapter config.
export default defineConfig({
  site: 'https://lilutm.netlify.app',
});
